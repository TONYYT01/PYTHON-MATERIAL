# Table of the contents
- [Opp Meaning](#oop-python)
- [What is class and Object](#what-is-the-class-)
- [Example](#example)
- [Implementing](#implementation-of-the-oop-in-the-python-coding)
- [4 pillers](#next--pillars-of-oop)

## OOP [PYTHON]
- OOPs is the one of the way to writing the code,modeling,design the real world entitys (objects) it contains the data and the behavior

- It has and we need to understand the some of the concepts clearly
### What is the class ?
### What is the object ?

### Class 
- It is like the blue print ,design
- Why it is call it has the blue print means sofor example you have the blueprint of the some one house or you designed the blueprint so with the help of that blueprint you can bulid the your house and the any one from other country or the state, village they can build there own house with the help of this blue print
- so class is like of the stracture how owr object should be look and do some other stuff

### Object

- It is the real world thing
- So object is like with the help of the class We can build the object
- with the help of single class We can build the many number of the objects we can build
- It has the own data and the behavior
##### Overview with full clarity

##### Example
- for example take the car ,in that i will take the BMW and it has color,speed,engine,stop,start,move, this things and all so this are the car behaviours and the data
- Now we will devide it `car` is the `class`
- `Object` is `BMW`
- `Data` is the `color engine,speed`
- `Behavior` is the `Start,stop,move`
- `Class` is an `idea`
- `Object` is `reality`
- So things and all comming under the oops basics to understand the concept what actually it is

#### Implementation of the oop in the python coding

####  Creating the class

```python
class Class_Name:
    # Write the code hear
```
- With the help of class Keyword we can create the class

#### Creating the Object

```python
class Car:
    def Start(self):
        print("Started"0)
c1=Car() ->Creating the object
c1.start() -> caling the Object,class behavior, start
```

- So this is all about the class And the object how actually we used and the core idea of it

###### [Next → Pillars of OOP](4-pillars-of-oop.md)